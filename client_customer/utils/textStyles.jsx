import { View, StyleSheet, Text, TouchableOpacity } from "react-native"

const textStyle = StyleSheet.create({
    textColor: {
        color: '#603913',
    },
    
    title: {
        fontSize: 30,
    },

    subtitle: {
        fontSize: 20,
    },

    regularText: {
        fontSize: 15,
    }
})

export {textStyle};